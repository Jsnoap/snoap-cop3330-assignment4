/*
 *  UCF COP3330 Fall 2021 Assignment 4 Solution
 *  Copyright 2021 Jordan Snoap
 */

package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

// This test case tests behaviors #9-10!
public class addRemoveItemTest {

    public void addingTask() {
        /*
        This test case will test the ability of the App to add a task to the ListView.
        This will be done by passing in the parameters to make a task such as the description and due date
        and then comparing the String afterword that resembles the task made.
        If the String is the same as expected, this case should pass.
         */
    }

    public void removingTask() {
        /*
        This test case will test the ability of the App to remove a task from the ListView.
        Similarly to the remove list case, this case will create a task through the proper functions and
        then call the associated functions to remove a task.
        Then it will check for the existence of the previous task and if it doesn't exist, it passes.
         */
    }
}
